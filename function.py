"""
A program to demo what functions can do.
"""
# we start by defining a function "definition"
def add(a,b):				# 1st line is called the header
	return a + b

print(add(4,3))
print(add(2.4,5.6))

# We then "call" (use) our function
# and "pass" it "parameters" 
# todo: make a function that joins words w. a space " google search " python 